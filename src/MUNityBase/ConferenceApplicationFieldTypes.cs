namespace MUNityBase
{
    public enum ConferenceApplicationFieldTypes
    {
        Text,
        MultiLineText,
        Number,
        Markdown,
        HTML,
        Date,
        Time,
        DateTime,
        Check,
        Country,
        School
    }

    public enum ConferenceApplicationFormulaTypes
    {
        Delegation,
        Role
    }
}