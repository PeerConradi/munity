import { CreateSimulationRequest } from './create-simulation-request';

describe('CreateSimulationRequest', () => {
  it('should create an instance', () => {
    expect(new CreateSimulationRequest()).toBeTruthy();
  });
});
