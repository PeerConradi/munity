import { AbstractAmendment } from './abstract-amendment.model';

describe('AbstractAmendment', () => {
  it('should create an instance', () => {
    expect(new AbstractAmendment()).toBeTruthy();
  });
});
