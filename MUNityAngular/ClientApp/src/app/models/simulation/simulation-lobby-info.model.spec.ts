import { SimulationLobbyInfo } from './simulation-lobby-info.model';

describe('SimulationLobbyInfo', () => {
  it('should create an instance', () => {
    expect(new SimulationLobbyInfo()).toBeTruthy();
  });
});
