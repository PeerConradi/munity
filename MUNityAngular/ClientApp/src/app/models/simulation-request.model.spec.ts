import { SimulationRequest } from './simulation-request.model';

describe('SimulationRequest', () => {
  it('should create an instance', () => {
    expect(new SimulationRequest()).toBeTruthy();
  });
});
