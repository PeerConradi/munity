import { OperativeSection } from './operative-section.model';

describe('OperativeSection', () => {
  it('should create an instance', () => {
    expect(new OperativeSection()).toBeTruthy();
  });
});
