import { CreateDelegationRequest } from './create-delegation-request';

describe('CreateDelegationRequest', () => {
  it('should create an instance', () => {
    expect(new CreateDelegationRequest()).toBeTruthy();
  });
});
