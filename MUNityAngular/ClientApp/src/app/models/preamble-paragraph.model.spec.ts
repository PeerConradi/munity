import { PreambleParagraph } from './preamble-paragraph.model';

describe('PreambleParagraph', () => {
  it('should create an instance', () => {
    expect(new PreambleParagraph()).toBeTruthy();
  });
});
