import { ResolutionHeader } from './resolution-header.model';

describe('ResolutionHeader', () => {
  it('should create an instance', () => {
    expect(new ResolutionHeader()).toBeTruthy();
  });
});
