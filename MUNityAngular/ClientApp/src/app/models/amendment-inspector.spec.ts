import { AmendmentInspector } from './amendment-inspector';

describe('AmendmentInspector', () => {
  it('should create an instance', () => {
    expect(new AmendmentInspector()).toBeTruthy();
  });
});
