import { SimulationMessage } from './simulation-message.model';

describe('SimulationMessage', () => {
  it('should create an instance', () => {
    expect(new SimulationMessage()).toBeTruthy();
  });
});
