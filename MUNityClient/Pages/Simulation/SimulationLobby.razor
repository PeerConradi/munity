@page "/sim/lobby/{id}"

@inject Services.SimulationService simulationService;
@inject NavigationManager navigationManager;

@layout MUNityClient.Shared.EmptyLayout;

@if (_context?.Simulation == null)
{
    <MUNityClient.Shared.Bootstrap.Spinner DisplayText="Lobby wird geladen" />
}
else
{
    <MUNityClient.Shared.VirtualCommittee.Lobby.SimulationLobbyMain ViewModel="@_context"></MUNityClient.Shared.VirtualCommittee.Lobby.SimulationLobbyMain>
}


